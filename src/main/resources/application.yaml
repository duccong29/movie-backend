#spring.application.name=movie-backend

server:
  port: 8080

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/movies"
    driverClassName: "com.mysql.cj.jdbc.Driver"
    username: root
    password: 123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  servlet:
    multipart:
      enabled: true
      max-file-size: 1000MB
      max-request-size: 1000MB

  logging:
    level:
      movies: DEBUG
      org.springframework.messaging: DEBUG
      org.springframework.web.socket: DEBUG
      org.springframework.data.redis: DEBUG

  files:
    video: videos/

  file:
    video:
      hsl: videos_hsl/

  image:
    upload:
      dir: ./uploads/images
    max-size: 5242880
    allowed-formats: jpg,jpeg,png,webp

  cloudinary:
    cloud_name: "dgvylsy5z"
    api_key: "635935476999317"
    api_secret: "PX6X7nvRO7tQjA2i4FXbPYmkXgM"

  jwt:
    signerKey: "huVDmdfmV0UcHOvREnyywZdtALSjCfpeKGj+ig1ayuCFtenw9wex46hNiQHpO+rt"
    valid-duration: 86400 # in second
    refreshable-duration: 36000 # in seconds

  kafka:
    bootstrap-servers: localhost:9094
    consumer:
      group-id: movies
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "*"
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

  outbound:
    identity:
      client-id: ""
      client-secret: ""
      redirect-uri: "http://localhost:3000/authenticate"

  mail:
    host: smtp.gmail.com
    port: 587
    username: duccongsos@gmail.com
    password: ofwc jrbf urip ufkx
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
      mail.smtp.starttls.required: true

  elasticsearch:
    enabled: false
    uris: http://localhost:9200

  redis:
    host: localhost
    port: 6379
    password: redis123

